# World Cup Predictor V1 Tasks

**Design:** `.specs/features/world-cup-predictor/design.md`
**Status:** Implemented; external integration and browser smoke verification pending
**Execution note:** Before executing tasks, confirm the production URL, Keycloak host/admin access, local PostgreSQL availability, and whether commits should be made per task.

## Implementation Sequence

```text
Foundation:
  T1 -> T2 -> T3

Identity and business rules:
  T3 -> T4
  T1 -> T5
  T3 -> T6 -> T7

User prediction slice:
  T4 + T5 + T7 -> T8 -> T9 -> T10

Administration and scoring:
  T4 + T5 + T7 -> T11 -> T12

Competition views:
  T10 + T12 -> T13 -> T14

Release verification:
  T9 + T12 + T13 + T14 -> T15
```

The tasks are deliberately grouped into thin vertical slices. `[P]` means a task can be implemented in parallel once its dependencies are complete.

## Phase 1: Foundation

### T1: Add Required Tooling And UI Dependencies

**What:** Install Auth.js/Keycloak support, Prisma/PostgreSQL support, Zod, focused test tooling, and initialize only the shadcn/ui components used by v1.
**Where:** `package.json`, `pnpm-lock.yaml`, `components.json`, `components/ui/*`, `lib/utils.ts`, CSS updates generated by shadcn/ui.
**Depends on:** Specification approval.
**Requirements:** UI-01, AUTH-01.

**Done when:**

- [ ] Dependencies support Next.js 16, Tailwind CSS 4, Auth.js Keycloak, Prisma ORM 7, PostgreSQL adapter, Zod, and Vitest.
- [ ] shadcn/ui components are limited to those used in the design.
- [ ] No existing starter dependency is downgraded without a documented compatibility reason.

**Verify:**

```bash
pnpm lint
pnpm build
```

**Commit:** `build: add application dependencies and ui tooling`

### T2: Define Prisma Schema And Database Configuration

**What:** Add the complete three-model schema, enum values, Prisma ORM 7 configuration, and initial migration.
**Where:** `prisma/schema.prisma`, `prisma.config.ts`, `prisma/migrations/*`, `.env.example`.
**Depends on:** T1 and a local PostgreSQL connection string.
**Requirements:** MATCH-01, MATCH-02, PRED-01, STAT-02.

**Done when:**

- [ ] `User`, `Match`, and `Prediction` match the design schema.
- [ ] Database URL is read through `prisma.config.ts`, not committed.
- [ ] Unique constraints for `keycloakId`, optional email, and `(userId, matchId)` are migrated.
- [ ] No role column exists.

**Verify:**

```bash
pnpm exec prisma validate
pnpm exec prisma migrate dev --name init
pnpm exec prisma generate
```

**Commit:** `feat: add predictor database schema`

### T3: Create Server-Only Prisma Client

**What:** Configure the reusable Prisma client instance with the PostgreSQL driver adapter.
**Where:** `lib/db.ts`, generated Prisma output configuration.
**Depends on:** T2.
**Requirements:** MATCH-01, PRED-01.

**Done when:**

- [ ] Client imports from the explicit generated output directory.
- [ ] Database access is guarded by `server-only`.
- [ ] Development hot reload does not create unbounded Prisma clients.

**Verify:**

```bash
pnpm exec prisma generate
pnpm lint
pnpm build
```

**Commit:** `feat: configure prisma database client`

## Phase 2: Identity And Business Rules

### T4: Implement Scoring Function And Unit Tests [P]

**What:** Implement the pure score calculation module and comprehensive category/rounding tests.
**Where:** `lib/scoring.ts`, `tests/scoring.test.ts`, test configuration if required by T1.
**Depends on:** T3 for generated stage enum types.
**Requirements:** SCORE-01, SCORE-02, SCORE-03, RANK-01, STAT-03.

**Done when:**

- [ ] Every exact/winner/draw/wrong category returns expected integer points.
- [ ] Exact draws receive 100% and Round of 32 fractions use `Math.round`.
- [ ] Tests cover home and away wins.

**Verify:**

```bash
pnpm test -- scoring
pnpm lint
```

**Commit:** `feat: implement prediction scoring rules`

### T5: Implement Validation And Match Timing Rules [P]

**What:** Define Zod input schemas and effective-start/editability utility functions with unit tests.
**Where:** `lib/validation.ts`, `lib/match-rules.ts`, `tests/validation.test.ts`.
**Depends on:** T1; generated enum types may be used after T2.
**Requirements:** MATCH-02, PRED-03, PRED-04, ADMIN-02.

**Done when:**

- [ ] Prediction scores reject blank, negative, fractional, and out-of-range values.
- [ ] Match forms reject identical teams and invalid result/status combinations.
- [ ] Kickoff/status timing tests prove late predictions are closed.

**Verify:**

```bash
pnpm test -- validation
pnpm lint
```

**Commit:** `feat: validate match and prediction inputs`

### T6: Configure Keycloak Realm And OIDC Client [P]

**What:** Perform and document environment-specific Keycloak realm, role, client, redirect, mapper, audience, and test-user setup.
**Where:** Keycloak Admin Console; local untracked environment configuration; update project setup documentation if needed.
**Depends on:** Production/development URLs and Keycloak administrator access.
**Requirements:** AUTH-01, AUTH-02, AUTH-03.

**Done when:**

- [ ] Realm `world-cup-predictor`, roles `USER`/`ADMIN`, confidential client, and unique-email policy exist.
- [ ] Exact callback/logout URLs, standard flow, optional PKCE `S256`, roles claim, and access-token audience are configured.
- [ ] Test access tokens contain expected `sub`, user profile claims, audience, and realm roles.

**Verify:** Sign in with one `USER` and one `ADMIN` through Keycloak and inspect only development tokens/claims through a secure diagnostic tool.

**Commit:** No secret-bearing configuration commit. Commit documentation only if setup notes change.

### T7: Integrate Auth.js And Server Authorization Helpers

**What:** Implement Auth.js Keycloak flow, local user synchronization, token-derived role checks, the Auth.js handler, and reusable role/guard tests.
**Where:** `auth.ts`, `app/api/auth/[...nextauth]/route.ts`, `lib/authorization.ts`, `lib/auth-guards.ts`, `tests/authorization.test.ts`, optionally `proxy.ts`.
**Depends on:** T3 and T6.
**Requirements:** AUTH-01 - AUTH-04, ADMIN-01.

**Done when:**

- [ ] A valid Keycloak sign-in creates/updates a local user by `sub`.
- [ ] Token issuer/signature/expiration/audience are verified before reading `realm_access.roles`.
- [ ] Session exposes only needed identity/roles, not raw access tokens or secrets.
- [ ] `hasRole()`, `isAdmin()`, `isUser()`, `requireUser()`, and `requireAdmin()` work as designed.
- [ ] Expired token-derived sessions fail protected access.

**Verify:**

```bash
pnpm test -- authorization
pnpm lint
pnpm build
```

Then manually sign in with `USER`, `ADMIN`, and no-role test users.

**Commit:** `feat: integrate keycloak authentication and role guards`

## Phase 3: User Prediction Slice

### T8: Build Authenticated Shell And Login Page [P]

**What:** Replace starter content with navigation, login/sign-out experience, and protected root redirect behavior.
**Where:** `app/layout.tsx`, `app/page.tsx`, `app/login/page.tsx`, `components/shared/app-header.tsx`.
**Depends on:** T1 and T7.
**Requirements:** AUTH-01, AUTH-04, UI-01.

**Done when:**

- [ ] Public users can access `/login` and begin Keycloak sign-in.
- [ ] Authorized users can navigate to matches/ranking/profile and sign out.
- [ ] Admin link appears only for an admin session as a convenience, without replacing server authorization.

**Verify:**

```bash
pnpm lint
pnpm build
```

Manual sign-in/sign-out/navigation verification.

**Commit:** `feat: add authenticated application shell`

### T9: Implement Match Listing And Detail Reads [P]

**What:** Add safe match DTO queries and basic match list/detail pages without prediction writes.
**Where:** `lib/data/matches.ts`, `app/matches/page.tsx`, `app/matches/[id]/page.tsx`, `components/matches/match-card.tsx`.
**Depends on:** T3, T5, and T7.
**Requirements:** MATCH-01, MATCH-02, VIS-01, UI-01.

**Done when:**

- [ ] Protected users see scheduled/started/finished match cards and details.
- [ ] Final scores render only when present.
- [ ] Query DTOs contain no email, Keycloak ID, role token, or hidden comparison scores.

**Verify:**

```bash
pnpm lint
pnpm build
```

Seed or admin-create matches and inspect each status in the UI.

**Commit:** `feat: add protected match views`

### T10: Add Prediction Entry And Comparison Privacy

**What:** Implement owner-only prediction create/update before kickoff and comparison visibility after kickoff.
**Where:** `actions/prediction-actions.ts`, `components/matches/prediction-form.tsx`, `components/matches/predictions-table.tsx`, updates to `lib/data/matches.ts` and match detail page; focused server/data tests.
**Depends on:** T5, T7, and T9.
**Requirements:** PRED-01 - PRED-04, VIS-01 - VIS-03.

**Done when:**

- [ ] User can upsert exactly one own prediction before effective start.
- [ ] Server rejects writes after time/status cutoff regardless of UI state.
- [ ] Before kickoff, server response reveals only caller prediction; after kickoff, it exposes safe comparison rows.
- [ ] Unique database constraint remains a final duplicate safeguard.

**Verify:**

```bash
pnpm test
pnpm lint
pnpm build
```

Manual two-user before/after kickoff comparison.

**Commit:** `feat: add predictions with kickoff privacy rules`

## Phase 4: Administration And Scoring

### T11: Implement Admin Match Mutations [P]

**What:** Implement guarded create/edit/status/result Server Actions, including status transition rules.
**Where:** `actions/admin-match-actions.ts`.
**Depends on:** T3, T5, and T7.
**Requirements:** ADMIN-01, ADMIN-02, MATCH-01, MATCH-02.

**Done when:**

- [ ] Only `ADMIN` can call actions successfully.
- [ ] Admin can create/edit a scheduled match and progress permitted statuses.
- [ ] Invalid inputs/transitions return safe actionable messages.

**Verify:**

```bash
pnpm test
pnpm lint
```

Directly attempt each action using admin and user sessions.

**Commit:** `feat: add admin match mutations`

### T12: Build Admin Pages And Atomic Result Recalculation

**What:** Add admin UI and ensure finishing/correcting a result updates all prediction points in one transaction.
**Where:** `app/admin/matches/**`, `components/admin/match-form.tsx`, `actions/admin-match-actions.ts`, result recalculation tests.
**Depends on:** T4, T11, and T10.
**Requirements:** SCORE-03, ADMIN-01 - ADMIN-03, UI-01.

**Done when:**

- [ ] Admin pages are server guarded and provide create/edit/result controls.
- [ ] Finished result assigns calculated points to every related prediction.
- [ ] Correcting result replaces all dependent points atomically.
- [ ] User sessions cannot access pages or succeed with submitted admin actions.

**Verify:**

```bash
pnpm test
pnpm lint
pnpm build
```

Manual result entry and correction with known predictions.

**Commit:** `feat: add match administration and result scoring`

## Phase 5: Rankings And Personal Experience

### T13: Implement Ranking Aggregation And Page [P]

**What:** Compute ranking rows and current-user position with deterministic tie-breakers; render ranking table.
**Where:** `lib/data/ranking.ts`, `app/ranking/page.tsx`, `components/ranking/ranking-table.tsx`.
**Depends on:** T4, T10, and T12.
**Requirements:** RANK-01, RANK-02, UI-01.

**Done when:**

- [ ] Ranking displays position, name/avatar, points, exact count, and correct winner count.
- [ ] Tie-break ordering matches the specification, with ID only as final stable fallback.
- [ ] Signed-in row is highlighted and “My Position” is displayed.

**Verify:**

```bash
pnpm test
pnpm lint
pnpm build
```

Seed tied users to verify every tie-break level.

**Commit:** `feat: add ranking standings`

### T14: Implement Personal Statistics And Dashboard Cards [P]

**What:** Add `/me`, favorite-team update, and dashboard summary cards/upcoming/recent/ranking preview on `/matches`.
**Where:** `lib/data/statistics.ts`, `actions/profile-actions.ts`, `app/me/page.tsx`, updates to `app/matches/page.tsx`, `components/stats/*`.
**Depends on:** T10, T12, and T13.
**Requirements:** STAT-01 - STAT-03, UI-01.

**Done when:**

- [ ] User sees total points, exact predictions, correct winners, predictions, accuracy, favorite team, and best stage.
- [ ] Favorite-team mutation edits only the caller's profile.
- [ ] Empty-state and zero-denominator behavior is clear.
- [ ] Matches dashboard shows the requested three top metrics and summary lists.

**Verify:**

```bash
pnpm test
pnpm lint
pnpm build
```

Compare calculated UI values with seeded known predictions.

**Commit:** `feat: add personal statistics dashboard`

## Phase 6: Release Verification

### T15: Validate Complete V1 And Security Boundaries

**What:** Run automated checks and manually exercise all actor journeys in the configured development environment.
**Where:** Tests, implementation fixes only if verification identifies defects; update `.specs/project/STATE.md` and status markers when complete.
**Depends on:** T8 - T14.
**Requirements:** All.

**Done when:**

- [ ] Lint, tests, Prisma validation, and production build pass.
- [ ] USER, ADMIN, expired/no-role, cutoff, privacy, scoring, correction, tie-breaker, and statistics journeys pass.
- [ ] Secrets/tokens are absent from tracked source and logs.
- [ ] The roadmap and state reflect completed or deferred work accurately.

**Verify:**

```bash
pnpm exec prisma validate
pnpm test
pnpm lint
pnpm build
git diff --check
```

**Commit:** `test: validate world cup predictor workflows`

## Requirement Coverage

| Requirements | Tasks |
| --- | --- |
| AUTH-01 - AUTH-04 | T1, T6, T7, T8, T15 |
| MATCH-01 - MATCH-02 | T2, T5, T9, T11, T15 |
| PRED-01 - PRED-04 | T2, T5, T10, T15 |
| VIS-01 - VIS-03 | T9, T10, T15 |
| SCORE-01 - SCORE-03 | T4, T12, T15 |
| RANK-01 - RANK-02 | T4, T13, T15 |
| STAT-01 - STAT-03 | T2, T4, T14, T15 |
| ADMIN-01 - ADMIN-03 | T7, T11, T12, T15 |
| UI-01 | T1, T8 - T14, T15 |

**Coverage:** 25 of 25 requirements mapped to one or more implementation and verification tasks.

## Practical Delivery Notes

- A single developer can execute in order; parallel markers are conveniences, not required coordination.
- Keep each commit focused on one task where practical. Tool-generated shadcn files and a migration belong with the task that creates them.
- Do not add caching, job processing, match providers, or generalized abstraction while completing these tasks.
- External Keycloak setup is required for integration tests but does not justify storing realm exports or secrets in the repository.
