services:
  database:
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  app:
    ports:
      - "127.0.0.1:${APP_PORT:-3000}:3000"

volumes:
  postgres-data:
